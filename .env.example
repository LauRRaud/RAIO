# RA•IO Payload CMS configuration
#
# Local example:
# DATABASE_URL=postgres://raio:change-me@127.0.0.1:5432/raio
#
# Generate a strong secret with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
DATABASE_URL=
PAYLOAD_SECRET=
